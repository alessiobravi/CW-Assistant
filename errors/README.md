# Local crash reports

Place exported crash reports and minidumps in this directory when diagnosing a
local build. Diagnostic payloads are ignored by Git because they can contain
user names, local paths, machine identifiers, and station configuration data.

Record the reproducible cause and corrective action in the changelog, backlog,
tests, and relevant manuals. Do not commit the raw report itself.
