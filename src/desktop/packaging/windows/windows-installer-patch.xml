<?xml version="1.0" encoding="UTF-8"?>
<CPackWiXPatch xmlns:util="http://schemas.microsoft.com/wix/UtilExtension">
  <CPackWiXFragment Id="#PRODUCT">
    <!--
      Keep the finish-page launch choice visible but initially unchecked. The
      upgrade path below selects it by default after installation completes.
    -->
    <Property Id="WIXUI_EXITDIALOGOPTIONALCHECKBOXTEXT"
              Value="Launch CW Assistant" />
    <Property Id="WixShellExecTarget"
              Value="[INSTALL_ROOT]bin\cw-assistant-desktop.exe" />

    <CustomAction Id="CwaLaunchApplication"
                  BinaryKey="WixCA"
                  DllEntry="WixShellExec"
                  Impersonate="yes"
                  Return="ignore" />
    <CustomAction Id="CwaDefaultLaunchAfterClosingApplication"
                  Property="WIXUI_EXITDIALOGOPTIONALCHECKBOX"
                  Value="1" />
    <InstallUISequence>
      <Custom Action="CwaDefaultLaunchAfterClosingApplication"
              After="ExecuteAction">
        WIX_UPGRADE_DETECTED AND NOT REMOVE~=&quot;ALL&quot;
      </Custom>
    </InstallUISequence>

    <UI>
      <Publish Dialog="ExitDialog"
               Control="Finish"
               Event="DoAction"
               Value="CwaLaunchApplication">
        WIXUI_EXITDIALOGOPTIONALCHECKBOX = 1 AND NOT REMOVE~=&quot;ALL&quot;
      </Publish>
    </UI>

    <!--
      CPack's MajorUpgrade authoring sets WIX_UPGRADE_DETECTED. The standard
      execute-sequence WixCloseApplications action requests a graceful close
      and only terminates after the bounded wait expires. Keep that canonical
      action here instead of binding a separate UI action to a
      platform-specific WiX custom-action binary.
    -->
    <util:CloseApplication Id="CwaCloseRunningApplicationDuringUpgrade"
                           Target="cw-assistant-desktop.exe"
                           Description="CW Assistant must close before the update can continue."
                           Sequence="1"
                           CloseMessage="yes"
                           ElevatedCloseMessage="yes"
                           Timeout="15"
                           TerminateProcess="1"
                           RebootPrompt="no">
      WIX_UPGRADE_DETECTED
    </util:CloseApplication>
  </CPackWiXFragment>
</CPackWiXPatch>
